namespace Library.Left;

/// <summary>One of two types called Palette, so resolving the name has to refuse rather than pick.</summary>
public static class Palette
{
	public const string Name = "left";
}
